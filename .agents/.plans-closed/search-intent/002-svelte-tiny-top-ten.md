# Plan 002: Move the svelte-tiny comparison into the top ten

> **Executor instructions**: Follow every step and verification. Stop on a listed
> condition. Update `.agents/.plans/search-intent/README.md` when complete.
>
> **Drift check (run first)**:
> `git diff --stat a4c51b9..HEAD -- docs/src/lib/compare-data.ts docs/src/routes/compare docs/e2e/demo.test.ts`
>
> Revision 2026-08-27: re-baselined after Plan 001 passed and added the shared
> install-intent E2E coverage plus the canonical `/install` destination.
>
> Revision 2026-08-27: added `docs/src/routes/compare/[slug]/+page.svelte` to
> scope after execution proved it is the consumer-owned file that configures
> the existing ComparisonPageV2 CTA. Editing docs-kit remains out of scope.

## Status

- **Priority**: P1
- **Effort**: S (hours)
- **Risk**: LOW
- **Depends on**: `001-npm-install-intent.md`
- **Category**: direction
- **Planned at**: commit `a4c51b9`, 2026-08-27

## Why this matters

The supplied query `svelte-tiny-virtual-list` has 26 impressions at average
position 11. The correct page already exists and is indexed; a small relevance and
internal-discovery improvement may move it into the first results page. Preserve
the page's honest comparison posture: capture-and-redirect means satisfying the
competitor query first, then presenting the alternative, not misrepresenting the
page as the competitor's official site.

## Current state

- `docs/src/lib/compare-data.ts:128-170` defines the competitor page. Its default
  title is generated by docs-kit as
  `svelte-tiny-virtual-list vs Svelte Virtual List | Compare`.
- The description is accurate but roughly 50 words and likely too long for a search
  snippet; the opening is competitor-first but does not say “alternative.”
- `ComparisonPageV2` supports `Competitor.seoTitle` and uses `description` for the
  meta description, overview, and Article JSON-LD.
- `docs/src/lib/docsNav.ts:137-141` already links directly to this comparison.
- `docs/e2e/demo.test.ts` already contains a focused comparison-content test; extend
  it instead of creating a parallel file.

## Target search presentation

- Title: `svelte-tiny-virtual-list Alternative for Svelte 5`
- Meta description: `Compare svelte-tiny-virtual-list and @humanspeak/svelte-virtual-list for Svelte 5: sizing, dynamic heights, horizontal lists, scrolling, and infinite loading.`

## Commands you will need

| Purpose     | Command                                                          | Expected on success  |
| ----------- | ---------------------------------------------------------------- | -------------------- |
| Typecheck   | `pnpm --filter docs check`                                       | exit 0               |
| Focused E2E | `pnpm --filter docs test:e2e -- --grep "svelte-tiny comparison"` | selected test passes |
| Build       | `pnpm --filter docs build`                                       | exit 0               |
| Format/lint | `trunk fmt && trunk check`                                       | both exit 0          |

## Scope

**In scope**: `docs/src/lib/compare-data.ts`,
`docs/src/routes/compare/[slug]/+page.svelte`, and `docs/e2e/demo.test.ts`.

**Out of scope**: other competitor facts, comparison component layout, docs-kit,
redirects, backlinks, paid promotion, or changing claims without primary-source
verification.

## Git workflow

Suggested branch `advisor/002-svelte-tiny-top-ten`; conventional commit
`docs(seo): sharpen svelte-tiny comparison intent`. Do not push unless instructed.

## Steps

### Step 1: Extend the existing E2E test and make it red

In the existing `svelte-tiny comparison reflects its Svelte 5 snippet API` test,
assert the exact target title, description, canonical URL, one H1 containing the
competitor name, and a visible link to `/install` supplied by Plan 001.

**Verify**: focused E2E → FAILS on title/description and install link. If it passes,
STOP and reconcile drift.

### Step 2: Tighten competitor metadata and intent copy

Add the exact `seoTitle` to the `svelte-tiny-virtual-list` record. Rewrite its
`description` to the exact target description if docs-kit requires one shared value;
if that makes the visible overview too thin, first confirm whether the installed
`Competitor` type has gained a separate SEO-description field. Use a separate field
only if it exists—do not patch docs-kit. Preserve the current source-supported
feature matrix, pros, cons, and verdict.

Ensure a crawlable `/install` link is visible on this page. Prefer configuring an
existing ComparisonPageV2 CTA/footer prop over adding custom markup. Use descriptive
anchor text such as `install the Svelte 5 alternative`, not the competitor's package
name alone.

**Verify**: focused E2E → passes.

### Step 3: Confirm SSR output and full quality gates

Build and inspect the prerendered page to ensure the SSR title/description/canonical
match and Article JSON-LD remains valid JSON. Run typecheck, full docs E2E, format,
and lint.

**Verify**: all commands exit 0; no other competitor record changed.

## Test plan

The existing comparison E2E is the regression anchor. Its new metadata assertions
must fail on the current default title/long description, then pass. No ranking test
is possible in the repository; use the batch README's 14/28-day Search Console
measurement.

## Done criteria

- [ ] Exact title and concise description are present in SSR HTML.
- [ ] Canonical remains `/compare/svelte-tiny-virtual-list`.
- [ ] Page H1 visibly names `svelte-tiny-virtual-list`.
- [ ] Existing factual comparison assertions still pass.
- [ ] A descriptive, crawlable next-step link reaches `/install`.
- [ ] Build, docs checks/E2E, and Trunk pass.
- [ ] Batch README status is updated.

## STOP conditions

- Source verification contradicts an existing comparison claim; report the claim
  separately instead of silently changing scope.
- The link requires editing docs-kit or invalid nested `<main>` markup.
- Search Console shows another canonical URL already owns this exact query after
  the plan date; report before changing canonical strategy.

## Maintenance notes

Recheck competitor claims before future copy changes. Do not churn metadata during
the first 28-day measurement window unless indexing is broken or the wrong page is
being served.
