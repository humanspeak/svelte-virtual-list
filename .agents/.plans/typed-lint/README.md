# Implementation Plans — typed-lint

Generated by the improve skill on 2026-07-14 (advisor session, commit `f89fd65`).
Origin: operator asked to "prepare" svelte-markdown's closed plan
`streaming-component-hardening/009` (executed and DONE in that repo) **for this
repo**, which has the identical two lint blind spots: test files excluded from
linting entirely, and no type-aware rules (`no-floating-promises`,
`no-misused-promises` inactive).

Single-plan batch. The plan is fully self-contained — the target config shape
from the exemplar repo is inlined; the executor must not read the other
repository.

## Execution order & status

| Plan | Title                                             | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Lint test files + enable type-aware promise rules | P2       | M      | — (note)   | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED
(with one-line rationale)

## Dependency notes

- No hard dependencies. **Cross-batch soft ordering**: prefer to execute after
  the `perf-parity` batch (001/002, in flight on 2026-07-14) merges — those
  plans create new test files that should receive lint coverage, and running
  afterward triages everything once. Running first is safe (disjoint files)
  but may leave one-time follow-up triage.

## Adaptation notes (vs the svelte-markdown original)

- Same core moves: un-ignore `**/*.test.ts` with a test override, swap to
  `recommendedTypeChecked` + `projectService: true`, explicit promise rules,
  `disableTypeChecked` carve-outs for JS and root-level TS configs, svelte
  parser wiring with `svelteConfig` + `extraFileExtensions`.
- Repo-specific: this workspace's `docs/` package has its own
  `eslint.config.mjs` and is explicitly out of scope; the five expected
  production violation sites in `src/lib/SvelteVirtualList.svelte` are
  pre-identified in the plan's triage table (lines 413, 439, 847, 858,
  947–954 at `f89fd65`).
- The original's guard revision (Trunk commands, not raw eslint) is baked in
  from the start — all verification runs through `trunk check` /
  `trunk check --all`.
